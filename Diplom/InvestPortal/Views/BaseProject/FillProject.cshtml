@model Invest.Common.Model.ProjectModels.Project
@{
    ViewBag.Title = "Заполнить проект";
}
<h2>Заполнить проект</h2>
<div>
    @Html.ActionLink("К моим проектам", "Index", null, new { @class = "icon-arrow-left-3 fg-darker big" })
</div>
</br>
<div class="balloon top">
    <p>Необходимо заполнить проект всей необходимой информацией.</p>
    <p>Так же крайне важно проверить адрес так, как не известно является ли он верным и отобразится на карте.</p>
    @if (Model.WorkflowState.CurrenState == Invest.Common.State.ProjectStates.Open)
    {
        <small>После сохранения проект отобразится на карте</small>
    }
</div>
<script>
    $(document).ready(function () {
        var link = '/BaseProject/@Model.GetType().Name' + 'Project/@Model._id';
        $.get(link, function (data) {
            $('#polymorphicEdit').html(data);
            $('#polymorphicEdit').show();
        });
    });
</script>
<style>
    #container {
        width: 95%;
    }

    #polymorphicEdit {
        float: left;
        margin-left: 10px;
        width: 99%;
    }
</style>
<div id="container">
    <div id="polymorphicEdit">Загружется...</div>
</div>
